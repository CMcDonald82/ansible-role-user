---

- hosts: localhost #all
  remote_user: root
  become: yes
  vars:
    system_users:
      - username: deploy_test
        password: admin
        groups:
          - sudo
        ssh_keys:
          # - ./tests/id_rsa.pub
          - /Users/chris/.ssh/id_rsa.pub
          
  roles:
    - role_under_test