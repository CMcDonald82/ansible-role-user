---

- hosts: localhost #all
  remote_user: root
  become: yes
  vars:
    system_users:
      - username: deploy_test_1
        password: admin
        groups:
          - sudo
        ssh_keys:
          - ./tests/files/id_rsa_test.pub
          - ./tests/files/id_rsa_test_2.pub

  roles:
    - role_under_test