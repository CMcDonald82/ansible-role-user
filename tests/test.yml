---

- hosts: localhost #all
  vars:
    system_users:
      - username: deploy_test
        password: admin
        groups:
          - sudo
        ssh_keys:
          - ~/.ssh/id_rsa.pub
          
  roles:
    - role_under_test